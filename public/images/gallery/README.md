# Gallery images

Drop any extra photos here — behind-the-scenes shots, event photos, work
samples, etc. — if you build a gallery section later. This folder is not
wired into any component by default.
